---
description: 3World 开发环境测试账号快速生成与身份认证（KYC）规则
globs: ["*"]
alwaysApply: false
---

# 3World 测试账号与 KYC 自动化生成技能

当用户要求创建、生成测试账号，或完成 KYC（标准身份认证 Level 1、高级身份认证 Level 2）时，直接执行本技能的自动化脚本。

## 适用场景与触发词
- 「帮我生成一个测试账号」
- 「创建一个高级身份认证账号」/「生成 Level 2 KYC 账号」
- 「创建一个完成了标准身份认证的账号」/「生成 Level 1 KYC 账号」
- 「生成一个已绑定手机的普通账号」

## 执行方式
在终端中执行对应的脚本：

```bash
# 1. 生成高级认证账号（Level 2，耗时约 35-45 秒，推荐默认）
node ~/project/ai-skills/skills/create-test-account/scripts/fast-create-account.mjs --level 2

# 2. 生成标准认证账号（Level 1，耗时约 18 秒）
node ~/project/ai-skills/skills/create-test-account/scripts/fast-create-account.mjs --level 1

# 3. 生成基础账号（Level 0，仅注册和绑定 +852 手机，耗时约 1.5 秒）
node ~/project/ai-skills/skills/create-test-account/scripts/fast-create-account.mjs --level 0
```

## 执行结果返回格式
脚本执行完成后，请提取日志中输出的以下信息并清晰呈现给用户：
- **登录邮箱** (Email)
- **登录密码** (Password，默认为 `Password123!`)
- **绑定手机号** (Phone)
- **认证等级与状态** (KYC Level 1 / Level 2)
- **充提币限额** (USDT Limits)
